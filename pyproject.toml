[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "evolute"
version = "0.1.0"
description = "Energy- and VOLUme-preserving Time integration Engine"
requires-python = ">=3.9"
dependencies = [
	"numpy",
	"scipy"
]
